import { TYPOGRAPHY } from "../../shared/typography";
import { COLORS } from "./colors";

const meta = {
  name: "dark",
  themeId: "dark",
  version: "1.0.0",
};

export { COLORS, meta, TYPOGRAPHY };

